#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    int N, searchElement, found = 0;
    scanf("%d", &N);
    int arr[N];
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &searchElement);
    for(int i = 0; i < N; i++) {
        if(arr[i] == searchElement) {
            found = 1;
            break;
        }
    }
    if(found) {
        printf("Found\n");
    } else {
        printf("Not Found\n");
    }
    
    return 0;
}
